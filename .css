body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: #f0f2f5; 
    display: flex;
    justify-content: center; /* 中央寄せ（水平） */
    align-items: center; /* 中央寄せ（垂直） */
    min-height: 100vh;
    margin: 0;
}

.quiz-container {
    width: 90%;
    max-width: 600px; /* PCでの最大幅 */
    background-color: #ffffff;
    border-radius: 12px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    padding: 20px;
    margin: 20px auto; /* 上下のマージンと左右の自動マージンで中央を維持 */
}

/* ヘッダーデザイン */
.quiz-header {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    padding-bottom: 15px;
    border-bottom: 1px solid #eee;
}

.category {
    background-color: #e0e0e0;
    padding: 4px 8px;
    border-radius: 4px;
    font-size: 0.9em;
    font-weight: bold;
    margin-right: auto;
}

.hearts {
    margin-left: 10px;
}

.heart {
    color: #ff4d4d;
    font-size: 1.2em;
}

.score-info {
    width: 100%; 
    display: flex;
    justify-content: space-between;
    margin-top: 10px;
    font-size: 0.9em;
}

.trophy {
    color: #ffd700;
}

/* 問題文のデザイン */
.question-section {
    padding: 30px 0;
    font-size: 1.2em;
    font-weight: 600;
    color: #333;
}

/* 選択肢ボタンのデザイン */
.options-section {
    display: flex;
    flex-direction: column; /* 縦に並べる */
    gap: 15px;
}

.option-button {
    width: 100%;
    padding: 15px;
    font-size: 1em;
    text-align: left;
    background-color: #f9f9f9;
    border: 1px solid #ddd;
    border-radius: 8px;
    cursor: pointer;
    transition: background-color 0.2s, border-color 0.2s;
    outline: none;
}

.option-button:hover:not(.disabled) {
    background-color: #e6f7ff;
    border-color: #91d5ff;
}

/* 正解・不正解時のスタイル */
.correct {
    background-color: #e6ffe6;
    border-color: #52c41a;
    color: #000;
    font-weight: bold;
}

.incorrect {
    background-color: #ffeded;
    border-color: #ff4d4f;
    color: #000;
    font-weight: bold;
}

.disabled {
    pointer-events: none; /* クリック操作を無効化 */
}
