/* 基本設定 */
body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    background-color: #f0f2f5;
    display: flex;
    justify-content: center;
    align-items: flex-start;
    padding: 30px;
    min-height: 100vh;
    box-sizing: border-box;
}

.todo-container {
    width: 100%;
    max-width: 500px;
    background-color: #ffffff;
    border-radius: 12px;
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.08);
    overflow: hidden;
}

/* ヘッダー */
.app-header {
    background-color: #4a90e2; /* テーマカラー */
    color: white;
    padding: 20px;
    text-align: center;
}

.app-header h1 {
    margin: 0;
    font-size: 24px;
}

/* 入力フォーム */
.todo-form {
    display: flex;
    padding: 20px;
    border-bottom: 1px solid #eee;
}

#todo-input {
    flex-grow: 1; /* 入力欄が幅いっぱいに広がる */
    border: 1px solid #ddd;
    border-radius: 8px;
    padding: 12px 15px;
    font-size: 16px;
    margin-right: 10px;
}
#todo-input:focus {
    outline: none;
    border-color: #4a90e2;
}

.add-btn {
    background-color: #4a90e2;
    color: white;
    border: none;
    border-radius: 8px;
    padding: 0 20px;
    font-size: 16px;
    font-weight: 600;
    cursor: pointer;
    transition: background-color 0.2s;
}
.add-btn:hover {
    background-color: #357abd;
}

/* タスク一覧 */
.task-list-container {
    padding: 20px;
}

#task-list {
    list-style: none;
    padding: 0;
    margin: 0;
}

.task-item {
    display: flex;
    align-items: center;
    padding: 12px 0;
    border-bottom: 1px solid #f0f0f0;
}
.task-item:last-child {
    border-bottom: none;
}

/* タスクテキスト */
.task-text {
    flex-grow: 1;
    font-size: 16px;
    color: #333;
    cursor: pointer; /* クリックで完了できるように */
    user-select: none; /* テキスト選択を防ぐ */
}

/* 完了済みタスクのスタイル */
.task-item.completed .task-text {
    text-decoration: line-through;
    color: #999;
}

/* 削除ボタン */
.delete-btn {
    background-color: #ff4757;
    color: white;
    border: none;
    border-radius: 50%; /* 丸いボタン */
    width: 28px;
    height: 28px;
    font-size: 14px;
    font-weight: bold;
    cursor: pointer;
    margin-left: 10px;
    flex-shrink: 0; /* ボタンが縮まないように */
    transition: background-color 0.2s;
}
.delete-btn:hover {
    background-color: #e03141;
}
